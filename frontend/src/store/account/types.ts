export interface AccountStateT {
  balance: string,
  balanceTokenName: string,
  demoPrintValue: string,
  isLoading: boolean;
  loggedIn: boolean;
}

export interface BalanceT {
  balance: string;
  tokenName: string;
  demoPrintValue: string;
}