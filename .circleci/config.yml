version: 2
jobs:
    test_dapp:
        docker:
            - image: node:8.7
        working_directory: ~/project/dapp
        steps:
            - checkout:
                  path: ~/project
            - run: yarn
            - run: sh test.sh
workflows:
    version: 2
    unit_test:
        jobs:
            - test_dapp
