version: 2
jobs:
    test_dapp:
        docker:
            - image: desmart/truffle:3.2
        working_directory: ~/project/dapp
        steps:
            - checkout
            - run: yarn
            - run: yarn run truffle test
